# Project Decisions

이 문서는 프로젝트를 진행하면서 확정한 설계 및 기술 결정을 기록한다.

## 2026-07-14: 1단계 기본 골격

* Python 패키지는 `src/lotto_analysis` 레이아웃을 사용한다.
* 프로젝트 메타데이터와 pytest 설정은 `pyproject.toml`에서 관리한다.
* 설정 및 경로 관리는 표준 라이브러리와 `pathlib`을 사용하며, 환경변수로 경로를 변경할 수 있게 한다.
* 정규화된 단일 회차 모델은 불변 `dataclass`로 표현한다.
* 단일 회차 수집기는 추상 기반 클래스 인터페이스로 정의하고, 실제 외부 데이터 출처 구현은 후속 작업에서 결정한다.
* logging은 표준 라이브러리를 사용하고 콘솔 출력을 기본으로 하며 파일 출력은 선택적으로 설정한다.

## 2026-07-14: 공식 단일 회차 데이터 출처

* 단일 회차 데이터는 동행복권 공식 결과 화면이 사용하는 JSON 엔드포인트 `/lt645/selectPstLt645InfoNew.do`에서 조회한다.
* 요청에는 `srchDir=center`와 `srchLtEpsd=<회차>`를 사용하고, 응답 목록에서 요청 회차를 다시 확인하여 정확히 한 건만 반환한다.
* 해당 엔드포인트의 공개 API 문서는 확인되지 않았으므로 응답 구조 변경 가능성을 전제로 명시적인 형식 오류를 제공한다.
* HTTP 클라이언트는 명세에 포함된 `requests`를 사용하며 URL, timeout, User-Agent는 설정으로 관리한다.
* macOS 기본 Python 3.9의 LibreSSL 호환성을 위해 `urllib3`는 1.26 계열로 제한한다.
* 네트워크 오류, 회차 없음, 응답 형식 오류를 서로 다른 수집 예외로 구분한다.

## 2026-07-14: 범위 수집과 원본 보존

* 최신 회차는 공식 결과 페이지의 `opt_val` 입력값에서 확인하며 임의의 미래 회차를 조회해 추측하지 않는다.
* 범위 수집은 회차별 독립 요청으로 실행하고 기본 요청 간격은 0.5초로 둔다.
* 원본 JSON은 요청 회차의 공식 레코드 한 건을 `data/raw/draw_NNNN.json`으로 저장한다.
* 한 회차의 알려진 수집·파일 오류는 실행 요약에 기록하고 다음 회차 수집을 계속한다.

## 2026-07-15: 복구 가능한 전체 수집

* timeout, 연결 오류, HTTP 429와 5xx만 자동 재시도하며 기본 최대 재시도 횟수는 3회다.
* 재시도 대기는 기본 0.5초에서 시작해 시도마다 두 배로 증가한다.
* 전체·범위 수집은 기존의 유효한 원본 파일을 기본적으로 건너뛰어 중단 후 이어받을 수 있게 한다.
* 증분 수집은 저장된 최대 회차 다음부터 최신 회차까지 수집한다.
* 누락 보완은 1회부터 최신 회차 사이의 없거나 유효하지 않은 파일만 수집한다.
* 기존 원본과 공식 레코드가 다르면 기존 파일은 유지하고 신규 레코드를 `data/raw/conflicts/`에 내용 해시 기반 파일명으로 저장한다.
* 충돌 결과에는 값이 달라진 필드명을 포함하며 자동으로 어느 쪽을 정본으로 선택하지 않는다.

## 2026-07-15: 수집 실행 이력

* 모든 CLI 수집 실행 결과는 `data/collection_history/`에 실행별 JSON 파일로 저장한다.
* 실행 이력에는 명령, 시작·종료 시각, 소요 시간, 대상 범위, 성공·건너뜀·실패 회차와 실패 사유를 기록한다.
* 최신 회차 조회처럼 회차 수집 전에 실행 자체가 실패한 경우에도 오류 이력을 저장한다.
* 실행 이력 파일은 기존 파일을 덮어쓰지 않으며 수집 원본과 마찬가지로 Git에 포함하지 않는다.

## 2026-07-15: 수집 진행률과 파일 로그

* 수집 진행 상태는 Service의 선택적 콜백으로 제공하고 CLI가 사용자 출력 형식을 담당한다.
* 긴 수집 실행은 약 1% 단위로 진행률을 표시하며 실패 상태는 즉시 표시한다.
* 애플리케이션 로그는 콘솔과 `logs/lotto-analysis.log`에 함께 기록한다.
* 파일 로그는 기본 5 MiB 크기와 백업 3개를 사용하는 크기 기반 회전 정책을 적용한다.
* 로그 경로, 최대 크기와 백업 개수는 환경변수로 변경할 수 있다.

## 2026-07-15: 사용자 확인 작업 관리

* 사용자가 직접 실행·확인·결정해야 하는 작업은 `docs/USER_CHECKLIST.md`에 누적 기록한다.
* 체크리스트에는 현재 상태, 실행 명령, 예상 확인 결과와 반복 운영 작업을 함께 기록한다.
* 개발 단계가 바뀌거나 새로운 사용자 판단이 필요해질 때 체크리스트도 함께 갱신한다.

## 2026-07-15: 단계별 코드 리뷰 기록

* 주요 개발 단계가 끝날 때 `docs/reviews/`에 범위, 발견 사항, 테스트와 종료 판단을 기록한다.
* 발견 사항은 `P0`부터 `P3`까지 우선순위를 부여하고, 해결하지 않은 항목은 다음 단계 전 체크리스트에 연결한다.
* 코드 리뷰와 코드 수정은 분리하며, 리뷰 요청만 받은 경우에는 발견 사항을 임의로 수정하지 않는다.

## 2026-07-15: 1단계 리뷰 보완

* 공식 원본의 정식 저장은 필수 필드 정규화와 도메인 모델 검증이 성공한 뒤 수행한다.
* 공식 정수 필드는 실제 정수 또는 명확한 10진 정수 문자열만 허용하며 실수의 암묵적 절삭을 금지한다.
* 정규화 모델은 날짜, timezone 포함 시각과 숫자 필드 타입을 실행 시점에 검증한다.
* CLI는 설정 해석 이후의 초기화·수집 오류를 실패 이력에 기록한다. 설정 자체를 해석할 수 없거나 이력 저장소에 접근할 수 없으면 원래 오류와 이력 저장 불가 사유를 터미널에 표시한다.
* 동시 프로세스 파일 잠금과 Windows CI는 현재 단일 사용자·단일 프로세스 1단계 범위 밖이므로 후속 작업으로 유지한다.

## 2026-07-15: SQLite 제외와 정제 CSV

* 초기 SQLite 구현은 제외하고 원본 JSON에서 정제 CSV를 생성한 뒤 기본 분석을 진행한다.
* 데이터베이스가 필요한 시점에는 SQLite 중간 구현 대신 PostgreSQL과 Repository 계층을 도입한다.
* 정제 작업은 매번 전체 원본을 검증하여 `data/processed/lotto_draws.csv`를 재생성한다.
* 잘못된 원본이 있어도 정상 회차는 CSV로 생성하고 오류·중복·누락은 `validation_report.json`에 분리한다. 오류나 누락이 있으면 CLI는 실패 종료 코드를 반환한다.
* 기존 공식 원본에는 수집 시각이 없으므로 CSV의 `collected_at`을 빈 값으로 유지하며 파일 수정 시각으로 추정하지 않는다.

## 2026-07-17: 1차 기본 통계 분석

* 분석 엔진은 저장 방식에 종속되지 않으며 `DrawRepository` 인터페이스로 정제 데이터를 받는다.
* 최초 구현은 `CsvDrawRepository`를 사용하고 PostgreSQL 도입 시 Repository 구현을 교체한다.
* 출현 비율은 해당 번호의 일반번호 출현 횟수를 분석 대상 회차 수로 나눈 값으로 정의한다.
* 미출현 기간은 선택한 분석 범위에서 해당 번호의 마지막 출현 이후 지난 회차 수로 정의한다.
* 저번호와 고번호는 `1~22 / 23~45`, 번호 구간은 `1~10 / 11~20 / 21~30 / 31~40 / 41~45`로 구분한다.
* 연속번호 개수는 정렬된 당첨번호에서 차이가 1인 인접 번호쌍의 개수다.
* 분석 결과는 실행 시 계산하고 아직 별도 파일이나 데이터베이스에 저장하지 않는다.
* 모든 분석 결과는 과거 데이터의 기술통계이며 예측이나 당첨 가능성으로 표현하지 않는다.

## 2026-07-17: PostgreSQL 전 분석 범위 완료

* 기본 분석은 합계의 최소·최대·평균·중앙값·표준편차, 홀짝·저고·구간·끝수 분포와 연속번호 비율을 요약한다.
* 기간 비교 기본값은 최근 N회와 직전 N회이며 선택적으로 전체 기간과 최근 N회를 비교한다.
* 기간 비교는 출현 횟수와 함께 회차당 출현률 차이를 사용한다.
* 번호 순위는 `1 + 자신보다 출현률이 높은 번호 개수`로 정의하여 동률 번호에 같은 순위를 부여한다.
* 출현 간격은 실제 회차 번호 차이를 사용하고 평균·중앙·최소·최대·최근 간격과 모집단 표준편차를 계산한다.
* 쉽게 재계산 가능한 분석 결과는 데이터베이스에 저장하지 않으며 사용자가 `--export`를 지정할 때만 `data/analysis/`에 JSON으로 저장한다.
* 번호쌍·3개 조합·행렬·조건 생성·백테스트는 PostgreSQL 도입 이후 단계로 둔다.

## 2026-07-17: Docker 기반 PostgreSQL 개발 환경

* 로컬 PostgreSQL은 Docker Desktop과 Docker Compose로 실행한다.
* PostgreSQL 17 공식 Alpine 이미지를 사용하고 최신 17.x 수정 버전은 이미지 갱신 시 반영한다.
* DB 포트는 외부 네트워크에 공개하지 않고 `127.0.0.1`에만 연결한다.
* DB 파일은 Compose 명명 볼륨에 보존하여 컨테이너를 다시 만들어도 유지한다.
* DB 이름, 사용자, 비밀번호와 호스트 포트는 `.env`에서 읽고 실제 `.env`는 Git에 포함하지 않는다.
* Compose 설정과 컨테이너 검증을 먼저 마친 뒤 SQLAlchemy, psycopg, Alembic과 Repository를 별도 작업으로 도입한다.

## 2026-07-17: PostgreSQL 스키마와 Repository

* PostgreSQL 연결과 ORM은 SQLAlchemy 2.x, 드라이버는 psycopg 3.x를 사용한다.
* 스키마 버전은 Alembic 마이그레이션으로 관리하며 애플리케이션 시작 시 암묵적으로 테이블을 생성하지 않는다.
* 정규화된 회차 데이터는 `lotto_draws` 한 테이블에 저장하고 회차 번호를 유일 키로 유지한다.
* 번호 범위·정렬·중복, 보너스 번호 중복과 음수 금액은 데이터베이스 제약조건으로도 방어한다.
* `PostgresDrawRepository`는 기존 `DrawRepository`를 구현하여 분석 엔진이 저장 방식에 종속되지 않게 한다.
* 최초 이관은 검증된 CSV 전체를 회차 번호 기준 upsert하고, 재실행해도 중복 행이 생기지 않게 한다.
* 이관 후 CSV와 PostgreSQL의 정규화 데이터 및 기본 분석 결과를 함께 비교한다.
* DB GUI는 무료 오픈소스와 다중 DB 지원을 우선해 DBeaver Community를 권장하되 필수 개발 의존성으로 두지 않는다.

## 2026-07-17: 관계 분석 1차 범위

* 관계 분석의 첫 범위는 일반 당첨번호 6개 사이의 번호쌍, 3개 조합과 특정 번호의 동반 출현으로 제한한다.
* 분석 Service는 `DrawRepository`에만 의존하고 CLI에서는 PostgreSQL Repository를 사용한다.
* 출현하지 않은 모든 가능한 조합을 0건으로 생성하지 않고 실제 등장한 조합만 반환한다.
* 조합 출현률은 해당 조합 출현 회차 수를 분석 대상 회차 수로 나눈다.
* 특정 번호의 동반 출현률은 상대 번호 동시 출현 횟수를 기준 번호 출현 횟수로 나눈 조건부 비율이다.
* 결과는 횟수 내림차순, 동률이면 번호 오름차순으로 정렬한다.
* 재계산 가능한 관계 분석 결과는 DB에 저장하지 않고 요청 시에만 JSON으로 내보낸다.
* 동시 출현 빈도와 조건부 비율은 과거 관계를 설명할 뿐 미래 당첨 확률이나 인과관계를 의미하지 않는다.

## 2026-07-17: 관계 분석 2차 범위

* 번호 간 거리는 한 회차의 일반번호 6개에서 만들어지는 15개 번호쌍의 절대 차이로 정의한다.
* 거리 비율의 분모는 분석 회차 수의 15배인 전체 번호쌍 관측 수다.
* `±1 관계`는 거리가 1인 번호쌍으로 정의하고 총 번호쌍 수와 하나 이상 포함한 회차 수를 함께 제공한다.
* 같은 끝수 관계는 두 번호를 10으로 나눈 나머지가 같은 번호쌍으로 정의한다.
* 연속번호 그룹은 한 회차 안에서 길이가 2 이상인 최대 연속 구간으로 집계한다.
* 이전 회차 중복은 분석 범위 안에 정확한 `현재 회차 - 1·2·3` 회차가 있을 때만 비교한다.
* 이전 회차 중복은 0~6개 분포와 비교 대상당 평균 중복 개수를 반환한다.

## 2026-07-17: 보너스 번호 후속 출현 분석

* 한 회차의 보너스 번호가 정확히 1·2·3회 뒤 회차의 일반번호 6개에 포함됐는지 각각 집계한다.
* 분석 범위 안에 정확한 후속 회차 번호가 있을 때만 해당 기준 회차를 분모에 포함한다.
* 출현 비율은 후속 일반번호 출현 횟수를 비교 가능한 기준 회차 수로 나눈다.
* 보너스 번호와 같은 회차의 일반번호 관계나 3회를 초과한 누적 출현은 이번 범위에 포함하지 않는다.
* 결과는 과거 역할 전환 빈도이며 이후 번호 출현을 예측하는 지표로 사용하지 않는다.

## 2026-07-17: 7×7 번호 행렬 기본 분석

* 번호 1~45는 1부터 행 우선 순서로 7×7 고정 좌표에 배치한다.
* 마지막 행의 46~49 위치는 번호가 없는 빈 셀로 유지하고 집계에서 제외한다.
* 셀은 일반 당첨번호 출현 횟수와 분석 회차당 출현 비율을 제공한다.
* 행·열 통계는 번호 출현 횟수의 합계와 회차당 사용한 서로 다른 행·열 개수의 평균을 제공한다.
* 전체와 최근 N회는 같은 계산을 사용하며 CLI에서는 PostgreSQL Repository를 사용한다.
* 대각선과 두 기간 차이 행렬은 기본 행렬 검증 후 다음 범위에서 구현한다.
* 행렬 분포는 과거 데이터의 공간적 표현이며 미래 당첨 가능성을 의미하지 않는다.

## 2026-07-17: 행렬 대각선과 기간 비교

* 주대각선은 유효한 셀 `1, 9, 17, 25, 33, 41`, 역대각선은 `7, 13, 19, 25, 31, 37, 43`으로 정의한다.
* 대각선 통계는 번호별 합산 출현 수와 하나 이상의 대각선 번호를 포함한 회차 수·비율을 제공한다.
* 행렬 기간 비교의 기본 방식은 최근 N회와 바로 앞의 직전 N회다.
* 비교 결과는 49개 고정 셀마다 두 기간의 출현 횟수·출현률과 `최근 출현률 - 직전 출현률`을 기록한다.
* 두 기간을 온전히 구성할 수 없으면 비교하지 않고 오류를 반환한다.
* 출현률 차이는 과거 두 기간의 기술적 차이이며 추세나 미래 예측으로 해석하지 않는다.

## 2026-07-17: 조합 수학 특성 분석

* AC 값은 한 조합의 15개 번호쌍에서 얻은 서로 다른 양의 차이 개수에서 5를 뺀 값으로 정의한다.
* 번호 간격은 정렬된 일반번호 6개 사이의 인접한 5개 차이로 정의한다.
* 소수는 1~45의 표준 소수 14개이며 숫자 1은 소수와 합성수 어디에도 포함하지 않는다.
* 제곱수는 `1, 4, 9, 16, 25, 36`으로 정의하고 개수와 포함 여부를 함께 기록한다.
* 합계 구간은 `21-100 / 101-120 / 121-140 / 141-160 / 161-180 / 181-255`를 사용한다. 중앙부는 20 단위, 양쪽 희소 범위는 하나로 묶는다.
* 끝수 합계는 각 번호를 10으로 나눈 나머지 6개의 합으로 정의한다.
* 회차별 상세 특성과 전체 분포를 반환하고, 요청 시 JSON으로만 저장한다.
* 수학 특성은 이후 조합 필터에서 재사용할 수 있지만 그 자체가 당첨 확률을 높인다고 해석하지 않는다.

## 2026-07-17: 과거 당첨 조합 유사도

* 각 회차는 선택된 분석 범위 안에서 회차 번호가 작은 이전 회차들과만 비교한다.
* 전체 분포는 같은 회차쌍을 중복 계산하지 않는 순서 없는 조합으로 집계한다.
* 유사도는 공통 일반번호 개수와 Jaccard 지수 `교집합 크기 ÷ 합집합 크기`를 함께 사용한다.
* 회차별로 최대 중복 번호 수, 최대값을 공유하는 모든 이전 회차와 정확히 3·4·5·6개가 겹친 이전 회차 수를 기록한다.
* `recent N`은 최근 N회 내부 유사도만 계산하며 선택 범위 밖 데이터는 암묵적으로 포함하지 않는다.
* 전체 회차쌍 목록은 메모리에 보존하지 않고 집계값과 회차별 요약만 반환한다.
* 유사도는 조합 생성 시 과거 동일·유사 조합 제외에 활용할 수 있지만 당첨 가능성 점수로 해석하지 않는다.

## 2026-07-17: 최근 범위 초과 요청 처리

* 단일 기간 분석의 `recent N`이 실제 보유 회차 수보다 크면 전체 데이터로 대체하지 않고 오류를 반환한다.
* 오류에는 요청 건수와 실제 사용 가능한 회차 수를 함께 표시한다.
* 기본 분석, 출현 간격, 관계, 행렬, 조합 수학 특성과 유사도에 같은 기준을 적용한다.

## 2026-07-17: 조건 기반 번호 생성 1차 범위

* 생성 전략과 조건 필터는 별도 인터페이스와 모듈로 분리한다.
* 첫 전략은 Python `random.Random`을 사용하는 균등 무작위 비복원 추출이며 선택적 seed로 재현한다.
* 첫 조건 범위는 포함·제외 번호, 홀수, 낮은 번호, 합계, 소수, AC, 연속번호쌍과 과거·결과 조합 중복 제한이다.
* 낮은 번호는 기존 분석과 같은 1~22를 사용하며 짝수·높은 번호 개수는 홀수·낮은 번호 개수에서 파생한다.
* 기본적으로 과거 완전 동일 조합과 과거·생성 결과에 5개 이상 겹치는 조합을 제외한다.
* 기본 최대 시도는 10000회이며 미완료 시 생성된 결과는 유지하고 주요 거절 조건을 설명한다.
* 조건을 통과한 조합 사이의 근거 있는 우열 공식이 아직 없으므로 1차 범위에서는 점수를 만들거나 정렬하지 않는다.
* 생성 결과에는 홀짝·고저·합계·소수·AC·연속번호와 과거 최대 중복을 함께 제공한다.
* 생성 조합은 사용자 조건을 만족하는 후보일 뿐 추천 점수나 당첨 확률을 의미하지 않는다.

## 2026-07-17: 빈도 가중 번호 생성 전략

* 빈도 가중 전략은 전체 또는 최근 N회의 일반번호 출현 횟수를 사용한다.
* 기본 가중치는 `출현 횟수 + 1` additive smoothing으로 0 가중치를 방지한다.
* 각 가중치는 45개 번호 평균 가중치의 0.5배~1.5배 범위로 제한한다.
* 후보 6개는 제한된 가중치를 사용해 비복원 추출하며 필수·제외 번호와 기존 필터를 그대로 적용한다.
* seed, 가중치 원본 회차 수, smoothing과 cap 공식을 결과에 기록한다.
* 내보내기 파일명은 전략과 전체·최근 가중 범위를 포함해 같은 seed의 다른 전략 결과를 덮어쓰지 않는다.
* 빈도 가중치는 사용자 선택 전략이며 미래 당첨 확률 추정치로 표현하지 않는다.

## 2026-07-17: 생성 전략 백테스트 기본 구조

* 백테스트 목표 회차에는 해당 회차보다 작은 회차만 학습 데이터로 전달하여 미래 데이터 누수를 방지한다.
* 첫 범위는 균등 무작위와 전체·최근 N회 빈도 가중 전략을 지원한다.
* 모든 목표에서 동일한 회차당 조합 수를 사용하고 실제 생성에 실패한 개수도 숨기지 않는다.
* 목표별 seed는 `기준 seed + 목표 회차 번호`로 파생해 실행 순서와 무관하게 재현한다.
* 생성 조합별 일반번호 일치 개수와 보너스 번호 포함 여부, 목표별 최고 일치 수를 기록한다.
* 전체 조합 일치 분포와 목표별 최고 일치 분포는 서로 다른 지표로 분리한다.
* 과거 학습 범위는 `InMemoryDrawRepository`로 격리해 생성 Service가 목표·미래 회차에 접근하지 못하게 한다.
* 첫 범위에는 등수, 상금, 구매 비용과 수익률 계산을 포함하지 않는다.
* 조합 수와 seed가 다른 백테스트를 전략 성능 차이로 직접 비교하지 않는다.

## 2026-07-17: 생성 전략 반복 백테스트

* 균등, 전체 빈도 가중, 최근 N회 빈도 가중을 동일한 목표 회차·조합 수·seed 집합으로 반복한다.
* 기본 비교 조합 수는 1·5·10·50개, seed는 41·42·43이며 CLI에서 변경할 수 있다.
* 전략·조합 수별로 전체 조합의 일치 분포와 목표별 최고 일치 분포를 seed 전체에 걸쳐 집계한다.
* 평균 일반번호 일치 수와 평균 최고 일치 수를 함께 제공하되, 조합 수가 다른 행의 최고 일치 수를 전략 우열로 비교하지 않는다.
* 상세 회차 결과는 단일 백테스트 명령에 유지하고 반복 실험 JSON에는 비교용 요약만 저장한다.
* 백테스트 CLI의 등록·실행·출력 책임을 별도 명령 모듈로 분리하기 시작한다.

## 2026-07-17: Streamlit 초기 읽기 전용 UI

* 첫 UI 범위는 PostgreSQL 데이터 대시보드와 기본 번호 빈도 화면으로 제한한다.
* UI는 계산 로직을 갖지 않고 `DashboardService`와 기존 `AnalysisService`를 호출한다.
* 대시보드는 1회부터 최신 회차까지의 누락을 계산하고 최신 추첨·수집 정보를 표시한다.
* 기본 통계는 전체 및 최근 5·10·20·30·50·100회를 선택할 수 있게 한다.
* 수집, DB 수정, 번호 생성과 백테스트 실행 화면은 후속 범위로 둔다.
* Streamlit은 내부 분석 도구로 유지하며 최종 Next.js가 Python Service를 직접 호출하는 구조로 확장하지 않는다.

## 2026-07-17: Streamlit 패턴 분석 화면

* 패턴 화면의 첫 범위는 7×7 출현 행렬, AC 값, 합계 구간, 대각선과 연속번호 요약으로 제한한다.
* 전체 또는 최근 20·30·50·100회를 선택하고 동일 범위를 모든 패턴 결과에 적용한다.
* 기존 `AnalysisService`의 기본·행렬·패턴 결과만 표시하며 Streamlit에서 통계를 재계산하지 않는다.
* Streamlit 진입점은 화면 선택과 공통 초기화만 담당하고 화면별 렌더링 모듈을 분리한다.
* 번호쌍 히트맵과 기간 차이 행렬은 관계 분석 화면을 설계할 때 별도 범위로 추가한다.
* Streamlit 화면 모듈은 실행 위치에 영향을 받는 루트 패키지가 아니라 설치 대상 `lotto_analysis.ui` 아래에 둔다.

## 2026-07-17: Streamlit 관계 분석 화면

* 관계 화면은 전체 또는 최근 20·50·100회와 기준 번호 1개를 선택한다.
* 기준 번호 동반 출현, 상위 번호쌍·3개 조합, 거리, 직전 1·2·3회 중복과 보너스 번호 후속 출현을 표시한다.
* 상위 결과 표시 수는 5~30개 범위로 제한하고 집계 자체는 기존 `AnalysisService`에서 수행한다.
* 번호쌍 히트맵은 45×45 전체 시각화의 가독성을 별도로 설계해야 하므로 이번 표 기반 화면에는 포함하지 않는다.
* 관계 빈도와 조건부 비율은 과거 동시 출현 설명이며 번호 추천 점수로 표현하지 않는다.

## 2026-07-17: Streamlit 번호 생성 화면

* 생성 화면은 균등 무작위, 전체 빈도 가중과 최근 N회 빈도 가중 전략을 지원한다.
* 포함·제외 번호, 홀수·낮은 번호·합계·소수·AC 범위, 연속번호와 과거·결과 중복 제한을 입력받는다.
* 번호 목록과 seed는 UI에서 명시적으로 검증하고 실제 생성과 조건 검증은 기존 `GenerationService`와 모델에 위임한다.
* 위젯 변경마다 생성하지 않고 form 제출 시에만 실행하며 결과는 session state에 보존한다.
* 결과에는 전략, 생성 완료 수, 시도 횟수, seed, 조합별 특성과 조건 거절 횟수를 투명하게 표시한다.
* 첫 화면 범위에는 JSON·CSV 내보내기와 생성 이력 DB 저장을 포함하지 않는다.

## 2026-07-17: Streamlit 기간 비교와 출현 간격 화면

* 기간 비교는 최근 10·20·30·50·100회와 직전 동일 기간 또는 전체 기간을 비교한다.
* 기간 길이가 다를 수 있으므로 출현 횟수와 함께 회차당 출현률·차이·순위 변화를 표시한다.
* 출현 간격은 전체 또는 최근 20·30·50·100회를 같은 기존 계산으로 분석한다.
* 평균·중앙·최소·최대·최근 간격, 현재 미출현과 표준편차를 제공한다.
* 출현률 차이는 미래 추세로, 현재 미출현과 평균 간격은 다음 출현 시점으로 표현하지 않는다.

## 2026-07-17: Streamlit 유사도와 행렬 기간 비교 화면

* 유사도 화면은 최근 50·100·200·500회 내부의 순서 없는 과거 회차쌍을 비교한다.
* 공통 번호 개수 분포, 5개 이상 중복 쌍과 최근 회차별 최대 유사 이전 회차를 표시한다.
* 행렬 비교는 최근 20·30·50·100회와 바로 앞의 동일 길이 기간을 비교한다.
* 번호별 회차당 출현률 차이 차트, 7×7 고정 행렬과 상세 표를 함께 표시한다.
* 유사도와 출현률 차이는 과거 조합 설명이며 미래 성과나 추세로 표현하지 않는다.

## 2026-07-17: Streamlit 백테스트 화면

* 단일 상세 화면은 균등·전체 빈도·최근 N회 빈도 전략과 목표 수·조합 수·seed를 입력받는다.
* 전체 조합 일치 분포와 목표별 최고 일치 분포를 분리하고 목표별 학습 범위를 표시한다.
* 반복 비교 화면은 균등·전체 빈도·최근 빈도 전략을 선택한 조합 수와 seed 집합으로 동일하게 실행한다.
* 미래 데이터 차단과 집계 계산은 기존 Backtest Service에 위임하고 UI에서 재구현하지 않는다.
* 조합 수가 다른 행의 최고 일치 수를 전략 효과로 직접 비교하지 않도록 화면에 안내한다.

## 2026-07-17: Streamlit 내부 분석 도구 1차 마감

* 대시보드, 기본·패턴·관계·기간·간격·유사도·행렬 비교, 번호 생성과 백테스트 화면을 1차 범위로 확정한다.
* 사용자 지정 회차·날짜 범위, 45×45 번호쌍 히트맵, 수집·DB 관리와 결과 내보내기는 후속 개선으로 둔다.
* 다음 제품 단계는 기존 Service 계층을 사용하는 FastAPI이며 Streamlit은 내부 분석 도구로 유지한다.

## 2026-07-18: FastAPI 1차 기본 구조

* API는 `lotto_analysis.api` 설치 패키지 아래에 앱·의존성·라우터·스키마를 분리한다.
* 첫 범위는 health, 정제 회차 목록, 대시보드와 기본 분석 조회로 제한한다.
* API 라우터는 Repository와 기존 Service를 호출하며 통계 계산을 직접 구현하지 않는다.
* 응답은 DataFrame이나 내부 dataclass 암묵 변환이 아니라 명시적인 Pydantic 스키마로 검증한다.
* `recent=0`은 전체 범위이며 부족한 최근 범위는 기존 규칙과 동일하게 422로 처리한다.
* 도메인 입력 오류는 422, PostgreSQL 장애는 내부 접속 정보를 숨긴 503으로 응답한다.
* 테스트는 FastAPI dependency override로 인메모리 Repository를 주입해 PostgreSQL과 분리한다.

## 2026-07-18: FastAPI 기간 비교와 출현 간격

* 기간 비교는 필수 `recent`와 선택적 `against_all` 쿼리로 직전 동일 기간 또는 전체 기간을 기준으로 사용한다.
* 출현 간격은 다른 단일 범위 분석과 동일하게 `recent=0`을 전체 범위로 사용한다.
* 기간·번호별 결과는 명시적인 중첩 Pydantic 응답 스키마로 반환한다.
* 기간 비교와 간격 계산은 기존 `AnalysisService`에 위임하며 API 라우터는 입력과 응답 변환만 담당한다.

## 2026-07-18: FastAPI 고급 분석 조회

* 관계, 7×7 행렬, 행렬 기간 비교, 조합 수학 패턴과 과거 조합 유사도를 각각 조회 API로 제공한다.
* 관계 API의 기준 번호는 선택적 1~45이며 나머지 단일 범위 API는 `recent=0`을 전체로 사용한다.
* 행렬 비교는 필수 양수 `recent`를 최근·직전 동일 길이로 사용한다.
* 모든 중첩 결과는 전용 Pydantic 응답 스키마로 검증하며 기존 Analysis Service 계산을 재사용한다.
* 고급 분석 API 응답은 과거 기술통계이며 예측·추천 의미를 추가하지 않는다.

## 2026-07-18: FastAPI 번호 생성

* `POST /api/combinations/generate`는 균등·전체 빈도·최근 N회 빈도 가중 전략을 지원한다.
* 요청 스키마는 생성 개수, 포함·제외 번호, 홀짝·고저·합계·소수·AC와 중복 제한, seed를 명시한다.
* 전략 선택과 빈도 원본 검증은 `GenerationApplicationService`가 담당하고 라우터는 모델 변환만 수행한다.
* 응답은 완성 여부, 조합별 특징, 시도 횟수, 거절 사유와 빈도 가중 공식 정보를 포함한다.
* 같은 데이터·조건·seed는 같은 응답을 반환하며 생성 결과를 추천 순위나 당첨 확률로 표현하지 않는다.

## 2026-07-18: FastAPI 백테스트

* `POST /api/backtests/run`은 한 전략의 목표별 학습 범위·생성 조합·일치 결과와 집계를 반환한다.
* `POST /api/backtests/experiment`는 균등·전체 빈도·최근 빈도 전략을 동일한 조합 수와 seed 집합으로 반복한다.
* 미래 데이터 차단, 회차별 seed와 집계는 기존 Backtest Service가 담당하고 API는 전용 요청·응답 스키마를 제공한다.
* 단일 응답은 전체 조합 일치 분포와 목표별 최고 일치 분포를 분리하고 학습 종료 회차를 포함한다.
* 반복 응답은 전략·조합 수별 완료 실행과 평균을 제공하며 조합 수가 다른 최고값을 직접 비교하지 않는다.

## 2026-07-18: FastAPI 핵심 범위 연결 완료

* 회차·대시보드, 기본·고급 분석, 번호 생성과 단일·반복 백테스트를 기존 Service 계층에 연결했다.
* 다음 범위는 API 전체 코드 리뷰, CORS·운영 설정, 성능 한도와 선택적 PostgreSQL 통합 테스트다.
* 인증, 사용자별 저장과 비동기 작업 큐는 실제 Next.js·배포 요구가 정해질 때 결정한다.

## 2026-07-18: FastAPI 리뷰 보완과 단계 마감

* FastAPI lifespan 종료 시 생성된 SQLAlchemy 엔진을 dispose하고 설정·엔진 캐시를 비운다.
* 동기 번호 생성과 백테스트에 개수·시도·실험 grid의 API 상한을 적용한다.
* CORS 기본 origin은 Next.js 로컬 3000 포트이며 `LOTTO_CORS_ORIGINS`로 교체한다.
* 허용 CORS 메서드는 GET·POST·OPTIONS이고 인증 전이므로 credential은 허용하지 않는다.
* DB 오류는 내부 상세 없는 503으로 유지하고 자동 테스트로 검증한다.
* 상세 리뷰와 후속 P3는 `docs/reviews/2026-07-18-fastapi.md`에 기록한다.

## 2026-07-19: 회차 상세·최신·페이지 조회 API

* 기존 전체·최근 목록 API를 유지하면서 단일 회차, 최신 회차와 offset 기반 페이지 조회를 추가한다.
* 페이지는 회차 오름차순, 기본 50건, 최대 200건이며 전체 건수와 다음 페이지 존재 여부를 함께 반환한다.
* 존재하지 않는 회차와 데이터가 없는 최신 회차는 `404`로 처리한다.
* Repository는 상세·페이지·개수의 공통 기본 구현을 제공하고 PostgreSQL은 단건 조회, `LIMIT/OFFSET`, `COUNT` 쿼리로 재정의한다.

## 2026-07-19: 사용자 지정 기본 분석 범위

* 사용자 지정 범위의 첫 적용은 기본 통계 API로 제한하고 다른 고급 분석 확장은 실제 화면 요구에 맞춰 진행한다.
* 회차 시작·끝 또는 날짜 시작·끝 중 정확히 한 쌍을 사용하며 모든 경계값을 포함한다.
* 불완전하거나 두 방식을 혼합한 입력, 역방향 범위와 데이터가 없는 범위는 `422`로 처리한다.
* Repository에 회차·날짜 범위 조회 계약을 두고 PostgreSQL은 범위 조건을 SQL에서 실행한다.

## 2026-07-19: Next.js 사용자 화면 1차 골격

* `frontend`에 TypeScript·App Router 기반 Next.js 앱을 두고 Python 패키지와 독립적으로 실행한다.
* 첫 화면은 Server Component가 FastAPI만 호출하며 Python 모듈이나 PostgreSQL에 직접 접근하지 않는다.
* 첫 범위는 최신 회차, 데이터 현황, 최근 8회 기록과 최근 100회 번호 빈도 요약으로 제한한다.
* 별도 상태관리·UI·차트 라이브러리는 현재 화면에 필요하지 않아 추가하지 않는다.
* API 주소는 서버 환경변수 `LOTTO_API_BASE_URL`로 관리하고 연결 실패 시 실행 안내 화면을 제공한다.
* Next.js 16.2, React 19.2를 잠금 파일로 고정하고 도구 호환성을 위해 TypeScript 5.9와 ESLint 9를 사용한다.

## 2026-07-19: Next.js 회차 목록과 상세

* 회차 목록은 서버 렌더링과 URL `page` 쿼리를 사용하고 최신순 20건을 표시한다.
* FastAPI 페이지 API의 오름차순 계약은 유지하며 프런트엔드에서 현재 페이지 안의 표시 순서만 뒤집는다.
* 전체 건수로 offset과 마지막 페이지의 실제 요청 개수를 계산해 페이지 간 중복을 만들지 않는다.
* 상세 화면은 단일·최신 회차 API를 함께 사용해 존재하지 않는 다음 회차 링크를 노출하지 않는다.
* 공통 헤더와 푸터를 컴포넌트로 분리하고 대시보드의 해시 메뉴를 실제 회차 경로에 연결한다.

## 2026-07-19: Next.js 기본 통계 화면

* `/analysis`는 최근 프리셋, 회차 범위와 날짜 범위의 기본 통계 조회를 제공한다.
* 범위 선택은 별도 클라이언트 상태 없이 GET 폼과 URL 쿼리를 사용해 새로고침과 링크 공유가 가능하게 한다.
* 첫 분석 화면은 범위·합계·연속번호 요약, 출현 상위 10개와 1~45 번호 상세로 제한한다.
* 막대 표현은 CSS로 구현하고 현재 범위에 필요하지 않은 차트·폼·상태관리 라이브러리를 추가하지 않는다.
* API 검증 실패는 페이지 안의 범위 오류로 표시하고 출현·미출현 수치를 미래 확률로 표현하지 않는다.

## 2026-07-19: Next.js 기간 비교와 출현 간격 화면

* `/analysis/compare`는 최근 기간을 직전 동일 길이 또는 전체 회차와 비교하고 회차당 출현률 차이와 순위 변화를 표시한다.
* `/analysis/gaps`는 선택한 과거 범위에서 번호별 평균·최근 간격, 간격 범위와 현재 미출현 회차를 표시한다.
* 범위 선택은 기본 통계와 동일하게 GET 폼과 URL 쿼리를 사용해 새로고침과 링크 공유가 가능하게 한다.
* 분석 화면 사이의 이동은 공통 탐색 컴포넌트로 제공한다.
* 현재 규모에서는 별도 차트·폼·상태관리 라이브러리를 추가하지 않고 CSS 시각화와 Server Component를 유지한다.
* 기간 차이와 미출현 간격은 과거 기록을 설명하는 값이며 추세, 추천 또는 다음 출현 시점으로 표현하지 않는다.

## 2026-07-19: Next.js 고급 분석 1차 화면

* `/analysis/patterns`는 AC, 인접 간격, 소수·합성수·제곱수 개수와 합계 구간의 API 집계 결과를 표시한다.
* `/analysis/relationships`는 기준 번호 동반 출현, 번호쌍·3개 조합, 거리, 회차 중복과 보너스 후속 출현을 표시한다.
* 원시 회차 목록을 프런트엔드에서 다시 계산하지 않고 FastAPI가 반환한 기존 Analysis Service 결과만 사용한다.
* 관계 화면의 기준 번호와 두 화면의 분석 범위는 GET 폼과 URL 쿼리로 유지한다.
* 45×45 히트맵과 행렬 비교는 가독성과 상호작용 요구를 별도 설계할 후속 화면으로 둔다.
* 패턴과 관계 빈도는 과거 기술통계이며 추천 점수나 조건부 당첨 확률로 표현하지 않는다.

## 2026-07-19: Next.js 7×7 번호 행렬

* `/analysis/matrix`는 1~45를 고정된 7×7 위치에 배치하고 출현 횟수·회차당 비율, 행·열 합계와 대각선 결과를 표시한다.
* `/analysis/matrix/compare`는 최근 기간과 직전 동일 길이 기간의 번호별 회차당 출현률 차이를 같은 위치에서 표시한다.
* 46~49 위치는 데이터가 없는 빈 셀로 유지해 두 화면의 공간 위치를 동일하게 한다.
* 작은 화면에서는 행렬 자체를 축소하지 않고 가로 스크롤을 허용해 셀 숫자와 값을 읽을 수 있게 한다.
* 색상 강도는 선택 범위 안의 상대 빈도 또는 차이 절댓값이며 번호 우선순위나 미래 추세를 뜻하지 않는다.

## 2026-07-20: Next.js 과거 조합 유사도

* `/analysis/similarity`는 최근 50·100·200·500회에서 순서 없는 서로 다른 회차쌍의 공통 번호 개수 분포를 표시한다.
* 전체 회차는 비교 쌍이 급격히 증가하므로 첫 사용자 화면 프리셋에서 제외하고 최대 500회로 제한한다.
* 회차별 상세는 최신 20개 회차의 최대 중복 수, 최대 Jaccard 값과 가장 유사한 이전 회차 링크를 표시한다.
* 전체 쌍 분포와 개별 회차 최대값을 별도 영역으로 구분해 서로 다른 통계를 혼동하지 않게 한다.
* 조합 유사도는 과거 조합의 관계 설명이며 미래 당첨 가능성이나 추천 점수로 표현하지 않는다.

## 2026-07-20: Next.js 조건 기반 번호 생성

* `/generate`는 Next.js Server Action이 폼 입력을 FastAPI `POST /api/combinations/generate`에 전달한다.
* 브라우저는 FastAPI 주소를 직접 호출하지 않고 Next.js 서버를 통해 기존 Generation Application Service를 사용한다.
* 전략·개수·포함/제외 번호·seed는 기본 조건에, 나머지 범위와 중복 제한은 접을 수 있는 고급 조건에 둔다.
* 포함·제외 번호 문자열의 1~45 정수 형식과 중복 제거만 프런트엔드에서 처리하고 조건 충돌과 생성 판단은 백엔드에 위임한다.
* 완성 여부, 시도 횟수, seed, 조합 특징과 조건별 제외 횟수를 투명하게 표시한다.
* 결과는 조건을 만족하는 무작위 후보이며 추천 번호, 순위 또는 당첨 확률 향상으로 표현하지 않는다.

## 2026-07-20: Next.js 단일 전략 백테스트

* `/backtests`는 Next.js Server Action이 FastAPI `POST /api/backtests/run`을 호출한다.
* 기본 실행량은 최근 목표 20회와 회차당 5조합이며 API 상한인 총 5,000조합을 폼 제약과 안내에 반영한다.
* 전략, 빈도 학습 범위, 목표 수, 회차당 조합 수, 기본 seed와 최대 시도 횟수를 입력받는다.
* 전체 조합 일치 분포와 목표별 최고 일치 분포를 별도 영역으로 표시하고 서로 다른 분모를 명시한다.
* 각 목표의 실제 번호와 학습 시작·종료 회차를 표시해 학습 종료가 목표 회차보다 이전인지 확인할 수 있게 한다.
* 과거 일치 수는 전략의 미래 성과나 실제 당첨 확률로 표현하지 않는다.

## 2026-07-20: Next.js 반복 전략 비교

* `/backtests/experiment`는 Server Action을 통해 FastAPI `POST /api/backtests/experiment`를 호출한다.
* 기본 grid는 목표 20회, 조합 수 1·5·10개와 seed 41·42·43이며 총 예상 작업량은 2,880조합이다.
* 조합 수는 최대 4개, seed는 최대 10개이며 중복과 API의 총 100,000조합 상한을 검증한다.
* 균등, 목표 이전 전체 빈도와 최근 N회 빈도 전략을 조합 수별로 묶어 같은 조건끼리 표시한다.
* 평균 일반번호 일치와 목표별 평균 최고 일치를 분리하고 각 요약의 전체 분포를 펼쳐볼 수 있게 한다.
* 조합 수가 다른 행의 최고값을 전략 효과로 직접 비교하지 않으며 과거 평균을 미래 성과로 표현하지 않는다.

## 2026-07-20: Next.js 전체 코드 리뷰 보완

* 회차 상세 API의 실제 404와 FastAPI·PostgreSQL 장애를 구분해 각각 없는 회차와 서비스 상태 안내로 표시한다.
* 850px 이하 공통 헤더는 주요 메뉴를 숨기지 않고 두 번째 줄의 가로 스크롤 탐색으로 유지한다.
* 실제 상태 확인 없이 연결 성공을 단정하던 헤더 문구를 중립적인 `FASTAPI 연동`으로 변경한다.
* Server Action 폼 입력 복원, 프런트 E2E 테스트와 배포 관측성은 후속 P2·P3로 기록한다.
* 상세 리뷰 결과는 `docs/reviews/2026-07-20-nextjs.md`에 유지한다.

## 2026-07-22: Server Action 폼 입력 보존

* 번호 생성, 단일 백테스트와 반복 비교는 제출된 문자열 값을 action state에 함께 반환한다.
* 응답 후 form key를 갱신하고 제출값을 기본값으로 복원해 성공·검증 실패·API 실패에서 동일 조건을 유지한다.
* 체크박스의 해제 상태와 사용자가 입력한 목록 문자열도 정규화 전 원문 그대로 보존한다.
